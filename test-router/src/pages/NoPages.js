const NoPage = () => {
    return (
        <div>
            <h1>No Page Found</h1>
            <h3>
                404 error codes are generate when a
                user attempts to acces a webpage that
                does not exist, has been moved, or has a
                dead or broken link. The 404 error code
                is one of the most frequent errors a web
                user encounters. Servers are required to 
                reposnd to client request, such as when 
                a user attempts to visit a webpage.
            </h3>
        </div>
    );
};

export default NoPage;